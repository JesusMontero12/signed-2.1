function modalconfig(config){
    if(config == true){
        alert("Tu configuracion de geolocalizacion esta lista");
    } else {
        alert("aqui se implementara una ventana modal para configurar las coordenadas de tu empresa");
    }
}